services:
  monotes:
    container_name: monotes-container
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 9898:8000
    volumes:
      - monotes-data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "/app/curl", "-f", "http://0.0.0.0:8000/health"]
  
volumes:
  monotes-data:
    driver: local

networks:
  default:
    name: monotes-network
    external: false